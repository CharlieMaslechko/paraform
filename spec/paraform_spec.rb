# frozen_string_literal: true

RSpec.describe Paraform do
  it "has a version number" do
    expect(Paraform::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
